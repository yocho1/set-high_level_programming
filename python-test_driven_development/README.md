
n - Test-driven Development

## 📖 Description

This project focuses on **Test-Driven Development (TDD)** in Python. You'll learn how to write tests **before** writing code, use Python's testing tools (`doctest` and `unittest`), and handle errors gracefully.

## 📁 Files

| File | Description |
|------|-------------|
| `0-add_integer.py` | Function that adds two integers with type validation |
| `tests/0-add_integer.txt` | Doctest file for `add_integer` function |

## 🚀 How to Run

```bash
# Make the script executable
chmod +x filename.py

# Run the script
./filename.py
